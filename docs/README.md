# docs

This folder is for supplementary documentation — architecture deep-dives, ADRs, runbooks — that Claude Code reads on demand rather than automatically, unlike `CLAUDE.md` at the repo root which is loaded into every session.
